http://balazlee.ir, http://www.balazlee.ir {
    encode gzip
    
    handle_path /api/* {
        reverse_proxy server:3000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}